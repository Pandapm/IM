import * as React from 'react';

const NotFound = () => {
    let styles = {
        color: '#aaa',
    };

    return (
        <div style={{styles}}>此功能正在开发中...</div>
    );
};

export default NotFound;
